#### Set of Helper functions to compute all relevant parameters

extract_volume <- function(length, width, height){
  return(length * width * height)
}

extract_surface <- function(length, width){
  return(length * width)
}

extract_first_order <- function(ventilation, decay, deposition, control){
  ### TO DO
  return()
}

extract_ventilation_rate_per_person <- function(){
  ### TO DO
  return()
}
